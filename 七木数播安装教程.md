# 七木数播操作系统 (QMS Audio OS) v4.1.1 官方完整安装与激活教程

> **适用版本**：七木数播系统 ISO 4.1.1 纯净发烧正式版  
> **适用硬件**：主流 64 位 x86 设备（惠普 T620 等瘦客户机、工控机、Intel NUC、小主机、闲置笔记本及台式机等）  
> **阅读时间**：约 5 分钟（按步骤操作，10 分钟内即可完成全部部署与发烧开声）

---

## 目录
1. [准备工作](#一准备工作)
2. [第一步：制作 U 盘安装介质（写盘）](#第二步制作-u-盘安装介质写盘)
3. [第二步：主机接线与 BIOS 引导启动](#第三步主机接线与-bios-引导启动)
4. [第三步：进入 Live 临时试听模式（免写盘初体验）](#第四步进入-live-临时试听模式免写盘初体验)
5. [第四步：一键安装到机身固态硬盘（正式落盘）](#第五步一键安装到机身固态硬盘正式落盘)
6. [第五步：联网开通 3 天无限制深度试听](#第六步联网开通-3-天无限制深度试听)
7. [第六步：输入券码激活永久终身版（VIP 授权）](#第七步输入券码激活永久终身版vip-授权)
8. [发烧常见问题与贴士 (FAQ)](#八发烧常见问题与贴士-faq)

---

## 一、准备工作

### 1. 硬件清单
- **数播主机**：一台 64 位 x86 架构主机（建议配备至少 2GB 内存及一个 8GB 以上的内置硬盘/固态盘）；
- **安装 U 盘**：1 个容量 **4GB 及以上** 的空白 U 盘（注意：写盘会清空 U 盘数据，请提前备份重要文件）；
- **网线**：一根网线将数播主机连入家庭路由器/交换机（确保数播与您的控制手机/电脑处于**同一局域网/Wi-Fi**）；
- **音频解码设备**：一台 USB DAC 解码器或数字界面，通过 USB 线连接数播主机的 USB 接口；
- **临时显示器与键盘**（可选）：仅首次开机确认 IP 和按引导键使用，安装完成后完全无需显示器与键鼠（脱机纯盲开机）。

### 2. 软件文件
- **系统镜像**：官方安装包 `QMS-Audio-OS-v4.1.1-PRO.iso`；
- **官方写盘工具**：推荐使用 **Rufus**（Windows 平台，绿色免安装）或 **BalenaEtcher** / **Ventoy**。

---

## 二、制作 U 盘安装介质（写盘）

以 Windows 系统使用 **Rufus** 为例：

1. 将准备好的 U 盘插入电脑，打开 **Rufus** 软件；
2. **设备**：下拉菜单选择刚才插入的 U 盘；
3. **引导类型选择**：点击右侧【选择】按钮，找到并选中下载好的 `QMS-Audio-OS-v4.1.1-PRO.iso`；
4. **分区类型与目标系统**：
   - 分区类型：选 `MBR` 或默认；
   - 目标系统类型：选 `BIOS 或 UEFI`（七木数播自带双模自适应引导，新老电脑均通吃）；
5. 点击最下方的【开始】按钮；
6. **关键提示**：弹窗提示“检测到 ISOHybrid 镜像”或询问写入模式时，**保持默认勾选【以 ISO 镜像模式写入（推荐）】**，点击【确定】即可（无需切换为 DD 模式）；
7. 再次确认格式化提示，等待进度条走完显示“准备就绪”，安全拔出 U 盘。

---

## 三、主机接线与 BIOS 引导启动

1. **设备接线**：
   - 将制作好的安装 U 盘插入数播主机的 USB 接口；
   - 接好网线（另一端插路由器）；
   - 用 USB 屏蔽发烧线将 DAC 解码器连接至主机；
   - 临时接上显示器 HDMI/DP 线（以便直观观察开机状态）。
2. **开机并进入启动项菜单**：
   - 按下数播主机电源开机键，立刻连续轻按启动快捷键：
     - **惠普 (HP T620 等)**：按 `F9`
     - **联想 (Lenovo)**：按 `F12`
     - **戴尔 (Dell)**：按 `F12`
     - **华硕 / 技嘉 / 微星**：按 `F8` 或 `F11` 或 `F12`
     - **通用工控机 / 软路由**：按 `F11` 或 `F7` 或 `Esc`
3. 在弹出的启动项列表中，选中带有 **UEFI: [你的U盘名称]** 或 **USB Storage** 的项回车启动。

---

## 四、进入 Live 临时试听模式（免写盘初体验）

数播开机仅需数秒，显示器屏幕上会出现如下标准字符看板：

```text
================================================================================
          QMS Audio OS v4.1.1 PRO - Audiophile Master Edition
================================================================================
   Web Control : http://192.168.1.125/  or  http://qms.local/
   Audio DAC   : USB Audio DAC / XMOS / Amanero ...
   Engine Core : Bit-Perfect Low-Latency RAM Architecture
   System Mode : Live USB Mode (Read-Only / Ready to Install)
   Status      : Ready (Standing by for Bit-Perfect Playback)
================================================================================
   Open Web Control in your browser to manage music & installation.
================================================================================
```

1. **打开控制台**：
   拿出与数播在同一局域网内的 **手机、平板（iPad）或电脑**，打开任意浏览器（Safari、Chrome、Edge、微信等均可），在地址栏直接输入看板上显示的 IP（例如：`http://192.168.1.125`）或直接访问 `http://qms.local`；
2. **体验发烧界面**：
   浏览器瞬间秒开七木数播发烧黑金控制台；
3. **15 分钟免盘快速试听**：
   此时系统运行在高速内存中，**完全没有写入您的本机硬盘**。您可以立即试播系统内置的高码率示范母带曲目，或者插上放有无损音乐的移动硬盘，确认 DAC 正确识别出声、感受纯净的底噪与麦景图 4K 动圈表头。

---

## 五、一键安装到机身固态硬盘（正式落盘）

确认设备与声音一切正常后，建议将系统直接写入机身内置固态硬盘，脱离 U 盘独立开机：

1. 在 Web 控制台界面中，点击弹窗的 **【一键安装到机身固态硬盘】**（或进入右上角设置中的【安装管理】）；
2. 系统会自动列出主机检测到的内置硬盘（例如：`sda (120GB SSD)` 或 `nvme0n1`）；
3. 勾选确认目标硬盘，点击 **【立即开始格式化并安装】**；
4. 底层引擎将在 **15 ~ 30 秒内** 自动完成全盘分区、UEFI/MBR 混合引导部署与高保真内核镜像解压；
5. 界面提示“🎉 安装成功！请拔下安装 U 盘并重启主机”；
6. 拔掉刚才的安装 U 盘，在页面点击【立即重启】（或手动长按主机电源键关机再开机）。
   > *从此主机彻底摆脱 U 盘，每次开机直接从内部固态极速秒启！*

---

## 六、联网开通 3 天无限制深度试听

硬盘启动后，系统自动由 Live 模式转入正式发烧运行态：

1. 手机/电脑重新打开控制台网址（`http://qms.local` 或主机 IP）；
2. 页面会自动弹出：**【请先申请 3 天无限制深度试听】** 弹窗；
3. 点击下方的 **【一键联网申请 3 天免费试听】** 按钮；
4. 系统将在 1 秒内与云端鉴权中心完成握手，并自动签发 **72 小时无限制官方全功能深度试听权限**；
5. 此时您可以：
   - 挂载家庭局域网 NAS / SMB 曲库；
   - 载入上千张 SACD ISO 镜像和 DSD 母带；
   - 享受麦景图发烧双表头 4K 超清全屏沉浸；
   - 体验文件夹顺序播放与全功能无缝切歌。

---

## 七、输入券码激活永久终身版（VIP 授权）

当 3 天深度试用期结束（弹出金色锁屏）时，或者在试用期间任何时候，您都可以随时通过购买的券码激活终身版：

### 方式 A：在线一键极速激活（最推荐）

1. 点击弹窗上的 **【👑 输入 6 位兑换券码激活终身版】**（或点击右上角【设置】⚙️ -> 【VIP 授权与激活】）；
2. 弹窗中会显示您主机的专属 **【机器码】**（例如：`QMS-A1B2C3D4`）；
3. 在下方的输入框中，输入向官方客服或店铺购买时获得的 **【6 位兑换券码】**（例如：`892631` 或专属券码）；
4. 点击 **【在线激活】** 按钮；
5. 系统直连官方云端核销平台，1 秒内完成证书签发并下发至本地固化：
   > 弹窗提示：**🎉 恭喜！云端授权核销成功！系统已永久升级为【七木终身尊享 VIP】！**
6. 激活成功！永久移除所有时间限制，终身享受后续固件 OTA 无感热升级！

---

### 方式 B：离线 U 盘导入激活（专为无外网的独立独立听音室设计）

如果您的音响室数播主机处于不连互联网的纯封闭内网环境：

1. 在数播 Web 界面【VIP 授权与激活】窗口中，记下屏幕上的 8 位 **【机器码】**；
2. 用手机或另一台联网电脑，在浏览器中打开七木官方授权自助中心：  
   👉 **`https://wx.284868.xyz/qms-auth/`**
3. 依次填入您的 **【机器码】** 和购买的 **【兑换券码】**，点击【立即兑换证书】；
4. 兑换成功后，点击下载官方加密授权文件 **`license.lic`**；
5. 将 `license.lic` 复制到任何普通 U 盘的**根目录**，将 U 盘插进数播主机；
6. 数播后台自动侦测热插拔并在 2 秒内完成验签激活，屏幕提示激活成功，即可拔下 U 盘！

---

## 八、发烧常见问题与贴士 (FAQ)

**Q1：安装完成后，主机脱离显示器，怎么知道数播的 IP？**
- **方法一（最简单）**：手机连接同一 Wi-Fi，在浏览器直接输入 `http://qms.local` 或 `http://qms-audio.local`，系统自带 mDNS 广播，无需输入数字 IP 即可秒开；
- **方法二**：登录家庭路由器后台，在“已连接设备”列表中查看名为 `qms` 或 `alpine` 的设备 IP。

**Q2：插上 USB DAC 后界面没有声音怎么办？**
1. 检查 DAC 解码器的输入源（Input Source）是否切换为 **USB** 输入档位；
2. 在七木数播 Web 控制台右上角【设置】⚙️ -> 【音频输出设备】中，确认已选中您的 USB DAC 名称，系统默认优先自动路由。

**Q3：我的老主机安装时找不到内置硬盘？**
- 开机进入电脑主板 BIOS 设置，将硬盘模式由 `RAID / Intel RST` 切换为标准的 `AHCI` 模式，保存重启即可识别。

**Q4：未来系统有新版本（如功能优化、新表头样式），需要重新写盘安装吗？**
- **完全不需要！** 终身 VIP 用户享受专属 **免写盘 OTA 热升级**。
- 后续只需在 Web 控制台右上角点击【系统升级】，直接上传官方提供的升级包（`.qpkg`），10 秒内自动原子替换升级，您的歌曲收藏、播放列表、NAS 挂载配置全部完整保留！

---
*祝您在七木数播带来的纯粹声音世界里，重拾音乐最真挚的感动！*